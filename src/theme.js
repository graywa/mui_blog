import { createTheme } from '@mui/material'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#182312',
      light: '#832122',
    },
    secondary: {
      main: '#15C236'
    },
    otherColor: {
      main: '#534211'
    }
  }
})